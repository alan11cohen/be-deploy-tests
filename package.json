{
  "name": "order-pay-deployment",
  "version": "1.0.0",
  "description": "Order and Pay Backend Deployment",
  "scripts": {
    "postinstall": "cd order-pay-backend && npm install",
    "build": "cd order-pay-backend && npm run build",
    "start": "cd order-pay-backend && npm run start:prod",
    "start:prod": "cd order-pay-backend && npm run start:prod"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
